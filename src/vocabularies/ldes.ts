export default class LDES {
    private static readonly base = 'https://w3id.org/ldes#';
    public static eventStream = this.base + 'EventStream';
    public static timestampPath = this.base + 'timestampPath';
}