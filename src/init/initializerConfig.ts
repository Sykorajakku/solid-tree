import { FragmentationDescription } from "./fragmentationDescription"

export interface InitializerConfig {

    versionOfPath?: string
    
    shape?: string

    fragmentationDescriptions?: FragmentationDescription[]
}
